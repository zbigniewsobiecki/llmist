# Coverage map for spec 001-mcp-bidirectional

Auto-generated by `/z:plan`. Tracks which plans satisfy which spec ACs.

Parent spec: [001-mcp-bidirectional.md](../../specs/001-mcp-bidirectional.md)

## Spec ACs

| # | Spec AC (short) | Satisfied by | Status |
|---|---|---|---|
| 1 | Library agent uses one MCP server end-to-end (params, errors, budget) | plan 1 (foundation) | full |
| 2 | Multi-server with deterministic name-conflict resolution | plan 1 (single) + plan 2 (multi + conflicts) | partial chain |
| 3 | CLI TOML config for MCP servers | plan 1 (`--mcp-server` ad-hoc) + plan 2 (full `[mcp.servers.*]` schema) | partial chain |
| 4 | `llmist mcp import-claude-code` reads `~/.claude.json` and emits TOML | plan 2 (consumer-complete) | full |
| 5 | `llmist mcp serve` initialize handshake + `tools/call` works for any compliant client | plan 3 (exposer) | full |
| 6 | `--skills` option on `mcp serve` exposes MCP prompts (`prompts/list` + `prompts/get`) | plan 3 (exposer) | full |
| 7 | Published server passes MCP Inspector sanity checks (schemas valid, lists work) | plan 3 (exposer) | full |
| 8 | Claude Code installs the published server and calls a gadget end-to-end | plan 3 (exposer) | full |
| 9 | Allowlist by default + per-server `trust` opt-in for STDIO commands | plan 1 (foundation) | full |
| 10 | TUI execution-tree + logs show MCP origin (server name) for MCP-backed gadgets | plan 2 (consumer-complete) | full |
| 11 | MCP-backed gadgets honor the gadget permission policy | plan 2 (consumer-complete) | full |
| 12 | Zero overhead when MCP not used (`mcp/` module + SDK not loaded) | plan 1 (establishes invariant) + plan 2 (re-verified) | partial chain (invariant must hold across all plans) |
| 13 | Server crash / tool error / disconnect / timeout — agent continues | plan 2 (consumer-complete) | full |
| 14 | Process hygiene: no orphan child processes | plan 1 (basic — terminate on agent exit/abort) + plan 2 (signals + graceful shutdown) + plan 3 (exposer-side stdin-close + signals) | partial chain |
| 15 | Capability negotiation downgrades cleanly for unsupported primitives | plan 2 (consumer-complete) | full |
| 16 | 5-minute consume walkthrough + 10-minute expose walkthrough are end-to-end runnable | plan 1 (consume side) + plan 3 (expose side) | partial chain |
| 17 | E2E coverage: consume-only, expose-only, roundtrip, adversarial inputs | plan 1 (consume foundation) + plan 2 (multi/HTTP/prompts/error E2E) + plan 3 (expose + roundtrip E2E) | partial chain |

## Coverage summary

- **17 spec ACs** mapped to **3 plans**
- **11 full-coverage ACs** (one plan satisfies the AC end-to-end): #1 (P1), #4 (P2), #5 (P3), #6 (P3), #7 (P3), #8 (P3), #9 (P1), #10 (P2), #11 (P2), #13 (P2), #15 (P2)
- **6 partial-chain ACs** (multiple plans together satisfy the AC): #2, #3, #12, #14, #16, #17

## Plan dependency graph

```
                    ┌──→ 2-consumer-complete
1-foundation ───────┤
                    └──→ 3-exposer
```

- **Plan 1** is the prerequisite for both plan 2 and plan 3.
- **Plans 2 and 3 are siblings** — they can ship in parallel after plan 1 lands. Recommended landing order is 1 → 2 → 3 (consumer-side polish before exposer), but reviewers / contributors who want to parallelize can split 2 and 3 across reviewers.

## Plans

| # | File | Slug | Status |
|---|---|---|---|
| 1 | [`1-foundation.md`](./1-foundation.md) | `foundation` | pending |
| 2 | [`2-consumer-complete.md`](./2-consumer-complete.md) | `consumer-complete` | pending |
| 3 | [`3-exposer.md`](./3-exposer.md) | `exposer` | pending |

## Documentation impact distribution

Spec-level top-level docs are expanded to per-file impact across plans:

| Top-level doc (from spec) | Plan that owns it |
|---|---|
| `README.md` (capability bullet) | plan 1 introduces; plan 3 extends |
| `CHANGELOG.md` | each plan adds its own entry |
| `CLAUDE.md` | plan 1 introduces; plan 3 extends |
| Library docs — consume side | plan 1 introduces; plan 2 extends |
| Library docs — expose side | plan 3 |
| CLI docs — consume side (TOML, flags) | plan 1 (single-server flag) + plan 2 (TOML schema, import command) |
| CLI docs — expose side (`mcp serve`) | plan 3 |
| Security docs (allowlist, CVE context) | plan 1 |
| API reference | each plan adds entries for its own public types |
| Runnable examples | plan 1 (consume) + plan 3 (expose, roundtrip) |
| Configuration reference (TOML schema) | plan 2 |

No top-level doc is dropped; each is owned by exactly one plan as its primary author, with secondary edits where called out.
